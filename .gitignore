# Build + runtime
node_modules/
build/
.next/
out/
coverage/
*.log
*.tsbuildinfo

# Env
.env
.env.local
.env.*.local

# IDE / OS
.DS_Store
.idea/
.vscode/

# Secrets
*.pem
!*.pub
*.key

# NOTE: dist/ is intentionally COMMITTED so GitHub Actions can consume the
# bundled runner directly via `uses: sfg-labs/ai-reviewer-quality@main`.
